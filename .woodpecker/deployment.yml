steps:
  - name: Preconfigure
    image: node
    commands:
      - npm ci
  - name: Build
    image: node
    commands:
      - npm run build
      - sleep 5
  - name: Compress
    image: node
    commands:
      - tar -czf hackzogtum-webpage.tar.gz .
when:
  - branch: main